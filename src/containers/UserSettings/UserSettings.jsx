import React, { Component, PropTypes } from 'react'

export default class UserSettings extends Component {
  render () {
    return (
      <div className="container">
        <p>this is setting page for per user</p>
      </div>
    )
  }
}
